# Slice 1a: stdlib chunking + pytest
# Slice 1b: Ollama HTTP client for embeddings
# Slice 1c: Qdrant vector store
# Slice 1d: Ollama chat + RAG prompt / ask()
# Slice 1e: CLI index / ask
# Slice 2a: Tree-sitter Python symbol extraction
# Slice 2b: code-aware Python chunks
# Slice 2c: wire Python chunks + symbol payload into Qdrant
# Slice 2d: JS/TS/Go Tree-sitter extractors + suffix dispatch
# Slice 3a: in-memory BM25 lexical search over chunks
# Slice 3b: Reciprocal Rank Fusion of vector + BM25
# Slice 3c: wire hybrid retrieval into index payload + ask()
# Slice 4a: local cross-encoder reranker (sentence-transformers + torch)
# Slice 4b: hybrid candidate pool → rerank (no new deps)
# Slice 4c: wire reranker into ask() and the CLI
# Slice 5a: search_code tool wrapping retrieve_chunks (no new deps)
# Slice 5b: sandboxed read_file tool (no new deps)
# Slice 5c: get_symbol + find_references over indexed chunk metadata
# Slice 5d: search_documentation (BM25 over markdown/docs chunks)
pytest>=8.0.0
httpx>=0.27.0
qdrant-client>=1.12.0
tree-sitter>=0.23.0
tree-sitter-python>=0.23.0
tree-sitter-javascript>=0.23.0
tree-sitter-typescript>=0.23.0
tree-sitter-go>=0.23.0
sentence-transformers>=3.0.0
# transformers 5.x imports PIL.Image.Resampling (Pillow >= 9.1). 9.0.1 breaks
# CrossEncoder import and used to look like a missing sentence-transformers.
pillow>=10.0.0
